import React from "react";
import { Route, Routes } from "react-router-dom";

const Main = () => {
    return (
        <main>
            <Routes>
                <Route></Route>
            </Routes>

            </main>
    );
};
export default Main;